%h3 Reports
.bootstrap-table-dropdown-overflow
  %table.protocol_reports.custom_striped#reports_table{data: {toggle: 'table', search: "true", url: documents_path(document: { documentable_type: 'Protocol', documentable_id: @protocol.id }, format: :json), striped: "true", show: {columns: "true", refresh: "true", toggle: "true"}, "sort-name" => "created_at", "sort-order" => 'desc', pagination: 'true', 'page-size' => 20}}
    %thead
      %tr
        %th{data: {class: 'edit', align: "center", field: "edit"}}
          = t(:actions)[:edit]
        %th{data: {class: 'title', align: 'center', sortable: "true", field: 'title'}}
          = t(:documents)[:title]
        %th{data: {class: 'created_at', align: 'center', sortable: "true", field: 'created_at'}}
          = t(:documents)[:date_created]
        %th{data: {class: 'downloaded_at', align: 'center', sortable: "true", field: 'downloaded_at'}}
          = t(:documents)[:downloaded_at]
        %th{data: {class: 'state', align: 'center', field: 'state'}}
          = t(:documents)[:state]
        %th{data: {class: 'file', align: 'center', field: 'file'}}
          = t(:documents)[:file]
