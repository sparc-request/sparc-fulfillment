%h3= t(:participant)[:tracker]
#participant_tracker
  %table.participants{id: "participants-tracker-table", data: {toggle: 'table', search: "true", height: "400", url: "/protocols/#{@protocol.id}/participants.json", striped: "true", show: {columns: "true", refresh: "true", export: "true", toggle: "true"}, export: {types: ['excel','csv']}, select: {item: {name: "toolbar1"}}, sort: {name: 'last_name', order: 'asc'}}}
    %thead
      %tr
        %th{data: {align: "left", sortable: "true", field: "last_name", formatter: "nameFormatter"}}= t(:participant)[:last_name]
        %th{data: {align: "left", sortable: "true", field: "first_name", formatter: "nameFormatter"}}= t(:participant)[:first_name]
        %th{data: {align: "left", sortable: "true", field: "mrn"}}= t(:participant)[:mrn]
        %th{data: {align: "left", sortable: "true", field: "arm_name"}}= t(:participant)[:arm]
        %th{data: {align: "center", formatter: "calendarFormatter", width: "5"}}= t(:participant)[:calendar]
        %th{data: {align: "center", formatter: "statsFormatter", width: "5"}}= t(:participant)[:report]
        %th{data: {align: "center", formatter: "changeArmFormatter", width: "5"}}= t(:actions)[:change_arm]

.modal.fade#changeArmModal{tabindex: '-1', role: 'dialog', aria: {labelledby: "changeParticipantArm", hidden: 'true'}}
  #changeArmModalPlace
